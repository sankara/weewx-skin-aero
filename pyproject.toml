[project]
name = "aero-dev"
version = "1.1.2"
description = "Development tooling for the Aero WeeWX skin"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "weewx==5.1.0",
    "playwright>=1.40.0",
    "pytest>=7.0.0",
    "pytest-playwright>=0.4.0",
    "pytest-xdist>=3.0.0",
    "pywebpack>=1.0.0",
]

[project.scripts]
aero-gen = "aero_dev.data_generator:main"
aero-build = "aero_dev.report_builder:main"
aero-package = "aero_dev.packaging:main"
aero-bump = "aero_dev.versioning:main"
aero-snap = "aero_dev.screenshots:main"
aero-watch = "aero_dev.watch:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aero_dev"]
